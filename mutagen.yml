sync:
  defaults:
    # Default configuration for any sessions
    ignore:
      vcs: false           # Include repositories from Git, Subversion, etc. since Composer needs it
  frankenphp-sync:
    alpha: "."
    beta: "docker://frankenphp-test/app"
